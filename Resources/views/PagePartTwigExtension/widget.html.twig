{% for box in pageparts %}
    {% if box.controller %}
        {{ render(controller(box.controller, { 'pagePart': box })) }}
    {% else %}
        {% set resource = box %}
        {{ include box.defaultview }}
    {% endif %}
{% endfor %}