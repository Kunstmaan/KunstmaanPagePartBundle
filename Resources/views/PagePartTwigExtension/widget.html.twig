{% for box in pageparts %}
    {% if box.controller %}
        {{ render_esi(controller(box.controller, { 'pagePart': box })) }}
    {% else %}
        {% set resource = box %}
        {{ include box.defaultview }}
    {% endif %}
{% endfor %}